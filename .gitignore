node_modules/
.DS_Store
.env
.env.*
.compact/
coverage/
*.log
*.tsbuildinfo

# Proof server volumes
proof-server-data/

# UI build output — static export goes to ui/out (committed for Vercel)
ui/.next/

# API compiled output — rebuilt on Vercel via npm run build --workspace=api
# NOTE: We DO commit api/dist in CI so Vercel doesn't need to rebuild
# (remove the line below if you want to rebuild on Vercel)
api/dist/

# Wallet seed — NEVER commit this
*.seed
WALLET_SEED

# Docs
docs/deploy.log