/**
 * Nocturn Deploy Script
 *
 * For Level 2, we use the Lace Wallet browser extension to deploy and
 * interact with the contract on Preprod.
 *
 * Steps to deploy:
 *   1. Run `npm run dev`
 *   2. Open http://localhost:5173
 *   3. Connect your Lace Wallet (set to Preprod)
 *   4. Click "Deploy New"
 *   5. Approve the transaction in Lace
 *   6. Copy the displayed contract address into docs/preprod-address.txt
 *      and the README.md!
 */

console.log('🌑 Nocturn Deployment');
console.log('=====================');
console.log('For Level 2, contract deployment is handled directly from the UI');
console.log('so you can sign the deployment transaction with Lace.');
console.log('');
console.log('Run:  npm run dev');
console.log('Then click "Deploy New" in the browser.');